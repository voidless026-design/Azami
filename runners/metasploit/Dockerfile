# azami/metasploit — pinned Metasploit runner. Build: docker build -t azami/metasploit:latest runners/metasploit
# Uses the upstream framework image; the DockerRunner pins --entrypoint msfconsole.
FROM metasploitframework/metasploit-framework:latest
